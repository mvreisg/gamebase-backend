<!DOCTYPE html>
<html lang="en">
{% include "Components/Head.twig" with { 
    host: host,
    title: title,
} %}        
<body>
    <h1 class="m-1">{{ title }}</h1>
    {% include "Components/Login/Nav.twig" with { 
        host: host 
    } %}
    {% include "Components/Encryption/Jwt/JwtEncryption.twig" with { 
        returnCode: jwt.returnCode,
        output: jwt.output
    } %}       
    {% include "Components/Scripts/ClipboardScript.twig" %}    
    {% include "Components/Scripts/BootstrapScript.twig" %}
    {% include "Components/Scripts/SessionScript.twig" with { 
        host: host
    } %}    
</body>
</html>