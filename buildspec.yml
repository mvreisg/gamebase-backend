version: 0.2

env:
  variables:
    SODIUM_CRYPTO_SECRETBOX_KEY: b49cb41a2cff8db4f99ea27c7d80a4073337108c228535da506970fd1fbc34e8
    JWT_SECRET: batata

phases:
  install:
    commands:
      - composer install --dev
  build:
      commands:
      - echo "Listando variáveis de ambiente disponíveis..."
      - env
      - printenv
      - echo "Listando variáveis do $_SERVER no PHP..."
      - php -r 'print_r($_SERVER);'
      - composer format
      - composer lint
      - composer lint:fix  
      - composer test  
